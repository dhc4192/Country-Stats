import React from "react";

export default function CountryList({ countries }) {
  
//! Link each country to data

  return (
    <div className="CountryList">
        {/* {countries.map(country =>  */}
      {/* <p>{country.name}</p> */}
      <p>Country List</p>
       {/* )} */}
    </div>
  );
}
